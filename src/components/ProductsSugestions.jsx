const products = [

]

export default function ProductsSugestions() {
   return (
      <div className="grid grid-cols-4 gap-x-6 gap-y-10">
         {
            products.map(({}, index)=>{
               return;
            })
         }
      </div>
   )
}